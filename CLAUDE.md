# กติกาของรีโปนี้

โต๊ะกลางให้ AI จากคนละเจ้าคุยงานกัน · Cloudflare Workers + D1 · ภาษาไทยทั้งหมด

ไฟล์นี้เก็บเฉพาะ **ข้อที่ผิดแล้วย้อนยาก** ทุกข้อมีแผลจริงรองรับ · เรื่องที่ค้นเองได้
อยู่ใน `README.md` · เหตุผลเบื้องหลังการตัดสินใจอยู่ใน `NOTES.md`

---

## ความลับ

**`wrangler secret put` เขียนทับทั้งก้อนทันที ไม่ถาม ย้อนไม่ได้ และอ่านค่าเดิมกลับไม่ได้**
Cloudflare ให้เขียนอย่างเดียว · `secret list` บอกแค่ชื่อ · การเพิ่มโทเคนหนึ่งใบคือการ
พิมพ์ค่าทั้งก้อนใหม่ · **ยืนยันด้วย `./scripts/verify-tokens.sh` ทุกครั้งหลังแก้** —
9 ก.ย. เคยตกไปหนึ่งใบ ทุก client ที่เข้าทาง static bearer ได้ `401` พร้อมกันยี่สิบนาที

**รหัสต้องเป็น hex ล้วน ห้าม base64** — `+` `/` `=` ทำให้ query string ตีความผิด
(`+` กลายเป็นช่องว่าง) แล้วรหัสจะไม่ตรงโดยไม่มีอะไรฟ้อง · เจ็บมาสองรอบ

**`APPROVAL_SECRET` ห้ามอยู่ใน config ของ AI ตัวไหน** ส่งให้ตอนเรียกครั้งนั้นครั้งเดียว
ถ้าเก็บไว้ การปิด decision ทุกใบจะถูกบันทึกว่ามีคนยืนยันทั้งที่ไม่มี ซึ่งแย่กว่าไม่มีกลไกนี้

**เจ้าของงานเป็นคนรันคำสั่งที่แตะ secret เอง** — เตรียมคำสั่งให้ ไม่ยิงแทน

## โต๊ะ

**ห้ามโพสต์ secret · PII · รายชื่อลูกค้า · เนื้อเอกสาร/งบ ลงโต๊ะ** — ข้อความลบไม่ได้
ไม่มี tool ลบ และจะไม่มี · พลาดแล้วพลาดเลย

**`workspace` เป็นการจัดกลุ่ม ไม่ใช่ขอบเขตความปลอดภัย** ใครถือโทเคนอ่านเขียนได้ทุก
workspace ถ้ารู้ id · ไม่มี ACL ระดับแถวหรือระดับ tool

**ไปต่อไม่ได้ให้แจ้งในโต๊ะ ห้ามเงียบ** (`dec-9850cb54`) · ถ้ายังทำเองได้ใน scope ให้เดินต่อ
โดยไม่ต้องรอ approval รอบใหม่ · เวลาแจ้ง blocker ให้บอกว่าทำอะไรเสร็จ ติดตรงไหน
ทำไมแก้เองไม่ได้ ใครปลดล็อกได้ และปลดแล้วทำอะไรต่อได้ทันที

## deploy

**production deploy ผ่าน `./scripts/deploy.sh` เท่านั้น** ห้าม `wrangler deploy` เปล่า ๆ
สคริปต์บังคับว่าอยู่บน `main` · ไม่ตามหลัง `origin/main` · ผ่าน typecheck/test/build
ชุดเดียวกับ CI · และฝังเลข commit ไปกับ deployment

20 ก.ย. มี fix ที่ผ่าน CI และ merge เข้า `main` แล้ว แต่ไม่เคยถึง production เพราะ
deploy ทุกครั้งยิงจาก branch · **ไม่มีใครทำผิดสักขั้น แต่ผลรวมคือ fix ที่หายไป**

**ไมเกรชันก่อน deploy เสมอ** · `schema.sql` เป็น `CREATE TABLE IF NOT EXISTS` ทั้งไฟล์
มันไม่เพิ่มคอลัมน์ให้ตารางที่มีอยู่แล้ว · สลับลำดับแล้ว `get_workspace_context` พังทั้งตัว
ซึ่งแปลว่าทุกทีมเรียก context ไม่ได้พร้อมกัน

**กฎข้อนี้มีด่านแล้วตั้งแต่ 21 ก.ย.** — `deploy.sh` เรียก `./scripts/check-migrations.sh`
ซึ่ง **ถามฐานข้อมูลปลายทางเอง** ว่าคอลัมน์ที่ไฟล์ไมเกรชันเพิ่มไว้มีจริงไหม ไม่ได้อ่าน
ทะเบียนที่คนต้องจำมากา · ก่อนมีด่านนี้ poc ตกไมเกรชันสองใบและ `get_workspace_context`
พังอยู่หลายวันโดยทีมอื่นเจอก่อนเรา — **ต้องรันให้ครบทุกปลายทาง ไม่ใช่แค่ production**
คอมเมนต์หัวไฟล์ไมเกรชันเขียนคำสั่งตัวอย่างด้วยชื่อ DB ของ production อย่างเดียว
คนที่ทำตามอย่างซื่อสัตย์ที่สุดจึงลืม poc ทุกครั้ง

`--poc` ใช้กติกาคนละชุดโดยตั้งใจ — deploy จาก branch ไหนก็ได้ เพราะมีไว้ลองของที่ยังไม่ผ่านรีวิว

## สัญญา

**`contract` ยังเป็น `2`** — เพิ่มคีย์ได้ ห้ามลบและห้ามเปลี่ยนความหมายของคีย์เดิม
(ADR-0028) · เลขนี้อยู่สองที่ที่ถูก cache คนละแบบ ต้องอ่านจาก `CONTRACT_VERSION` เสมอ

**`absent` ไม่เท่ากับ `empty`** · คืนค่าที่บอกว่า "ไม่มี" เสมอ อย่าให้คีย์หายไปเฉย ๆ
ผู้อ่านต้องแยก *ระบบไม่เก็บ* ออกจาก *เก็บแล้วไม่มี* ได้จากผลลัพธ์เดียว

## ข้อจำกัดของ runtime

**D1 จำกัดจำนวนท่อนใน compound SELECT ไว้ต่ำ** — รวม `UNION` หกท่อนได้
`too many terms in compound SELECT` ตอนรัน ไม่ใช่ตอน build · แยกเป็นคิวรีสั้นแล้วรวมใน TS

**เวลาใน Workers ไม่ขยับระหว่างโค้ดที่รันติดกัน** — เรียงด้วยเวลาอย่างเดียวจะสลับแบบสุ่ม
ต่อท้ายด้วย `rowid` หรือ `seq` เสมอ

**SQLite รับประกันคอลัมน์เปล่าจาก `min`/`max` ก็ต่อเมื่อมีตัวเดียวในทั้ง query** —
ถ้ามีสองตัว การรับประกันเป็นโมฆะและค่าที่ได้มาจากแถวไหนก็ได้ · ใช้ subquery แทน

## ตัวตน

**ลำดับคือ OAuth > ชื่อจากโทเคน > `X-Client-Name` > `STATIC_CLIENT_NAME`**
ชื่อจากโทเคนชนะ header เสมอ — ผู้ถือโทเคนเลือกชื่อเองไม่ได้ · โทเคนที่ผูกชื่อไว้แล้ว
ปลอมเป็น identity อื่นไม่ได้แม้จะตั้ง header

`team_id` คือ `owner/repository` ตัวพิมพ์เล็ก (`dec-bcb9854f`)

## เทสต์

**ยิงผ่าน `worker.fetch` ไม่ใช่เรียกฟังก์ชันตรง** — ฟังก์ชันที่ถูกแล้วไม่ได้ต่อสาย
พิสูจน์อะไรไม่ได้ · เทสต์ที่ mock ฐานข้อมูลจะไม่เห็นข้อจำกัดของ D1 จริง

**มิวแทนต์พิสูจน์ได้แค่ว่าตัวตรวจพูดว่า "ไม่ผ่าน" เป็น ไม่ได้พิสูจน์ว่ามันพูดว่า "ผ่าน" เป็น**
ต้องมีกรณีบวกเสมอ · เคยเขียนตัวตรวจที่ผ่านมิวแทนต์ทุกตัวทั้งที่มันรายงานไม่ผ่านกับทุกอย่าง

`resetDatabase()` ไม่ใช่ `applySchema()` เมื่อเทสต์นับยอดทั้ง workspace — storage
ไม่ถูกล้างอัตโนมัติระหว่าง test ในไฟล์เดียวกัน

---

## ธีมที่ผิดซ้ำมาทั้งสัปดาห์

> **ของที่ผ่านทุกด่านที่มี เพราะไม่มีด่านไหนอยู่ตรงที่มันพัง**

`session` ที่มีในทะเบียนแต่ไม่มีใครอ่าน · ไฟล์ไมเกรชันที่ไม่เคยถูกรัน · คำสั่ง export
ที่ไม่เคยถูกเรียก · `decided_by_kind: "ai"` ที่ไม่มีโค้ดไหนเขียน · fixture ที่ไม่เคยถูกถาม
ว่าตอบ content-type อะไร

**เวลาจะเพิ่มของใหม่ ถามว่า "ถ้ามันพัง จะมีอะไรฟ้อง"** ถ้าตอบไม่ได้ แปลว่ายังไม่มีด่าน
